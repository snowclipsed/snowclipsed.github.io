<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://snowclipsed.github.io//me/feed.xml" rel="self" type="application/atom+xml"/><link href="https://snowclipsed.github.io//me/" rel="alternate" type="text/html" hreflang="en"/><updated>2024-01-28T17:49:02+00:00</updated><id>https://snowclipsed.github.io//me/feed.xml</id><title type="html">Hardik Bishnoi</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">CS6120 : Natural Language Processing | Week - 02</title><link href="https://snowclipsed.github.io//me/blog/2024/NLP-002/" rel="alternate" type="text/html" title="CS6120 : Natural Language Processing | Week - 02"/><published>2024-01-17T00:00:00+00:00</published><updated>2024-01-17T00:00:00+00:00</updated><id>https://snowclipsed.github.io//me/blog/2024/NLP-002</id><content type="html" xml:base="https://snowclipsed.github.io//me/blog/2024/NLP-002/"><![CDATA[<h1 id="text-representation">Text representation</h1> <p>NLP is all about making machines understand text. Since machines cannot understand text directly and do not speak in natural language but in binary, we need a method to transform or represent the text in a way such that machines can understand them. Such a technique is what call text representation.</p> <p>There are various methods to represent text with their own strengths and weaknesses. A few examples are bag-of-words, term frequency-inverse document frequency (TF-IDF), word embeddings, and more.</p> <h2 id="challenges-when-dealing-with-represented-text">Challenges when dealing with represented text</h2> <p>No text representation is perfect, but some are closer than others. Even so, there are different strengths and weaknesses to all of the methods listed above.</p> <ol> <li>Information loss The biggest challenge that we face when representing text is information loss. A sentence is way more than its semantics and verbiage. Text representation is a form of abstraction, and if it’s imperfect in abstracting, information encoded within the sentence can be lost. To capture all the information that is embedded in the sentence is a hard problem, specially since the information gained is often up to interpretation.</li> </ol> <p>For example when we use Bag of Words or TF-IDF representation, it does little to capture the context and order of the sentence since it only captures the frequency of words in a corpus - but not the semantic and contextual relationship between the words.</p> <ol> <li>Vocabulary size and Sparsity The english language contains about a million words. Representation of such a high amount of words can be really hard, because often it is a higher dimensional representation.</li> </ol> <p>Although our day-to-day lives use a very limited section of this total number, it can still be a considerable amount to invoke the curse of dimensionality when some approaches are used. Some corpora of text like Wikipedia may deal with thousands of unique words.</p> <p>The curse of dimensionality is a situation when the number of features or dimensions grow exponentially with respect to the size of the dataset, causing sparseness to occur : very high number of features with each feature having very small number of samples. A lot of naive methods like BoW and TF-IDF are subject to falling for this curse because they concern with learning frequencies of each word within a corpora.</p> <ol> <li>Ambiguity</li> </ol> <p>Ambiguity refers to a situation where a word, phrase or a whole sentence may have different contextual interpretations or meanings. Ambiguity is common and can arise in natural language due to homonymy, polysemy or when we are concerned with a whole sentence, syntactically where a sentence can be interpreted in multiple ways given no additional context.</p> <p>Ambiguity is a major problem in NLP since it can lead to difficulties in the machine understanding the true contextual meaning of the text. For example, consider the word “bank.” Without context, this word could refer to a financial institution, the side of a river or lake, or even a type of aircraft. Similarly, the sentence “She gave him her book” could be interpreted as giving away a physical object or selling access to digital content.</p> <p>Ambiguity can also occur due to the semantic similarity of two pieces of text. Words with similar meanings do not occur in the same order in the sentence, hence making it difficult for a model to generalize the semantic and contextual relationship of such words using traditional approaches like BoW.</p> <p>To combat this, different NLP models use techniques to remove ambiguity as much as possible. Some of the approaches are dependency parsing and named entity recognition. Modern probabilistic models are better than traditional ones when disambiguating text.</p> <ol> <li>Out-of-Vocabulary Words (OOV) This is a common problem that machine learning approaches face all the time : data that is previously unseen and unheard of. This can happen when the model is trained on a limited dataset or when the dataset does not include all possible variations of a word. OOV words can cause issues in NLP models because they may be difficult to represent or classify accurately.</li> </ol> <p>There are several techniques to deal with this problem, such as subword embeddings, byte-pair encoding and zero-shot approaches.</p> <ol> <li>Named Entity recognition (NER) It is the task of finding and categorizing named entitites inside a corpus of text. Named entitites include entities such as organizations, locations, and people. Named entities are often important contextual keys in sentences. It is hard to recognize named entities because of their contextual placement within sentences changes with different text examples and languages. NER is hence not easily managed by statistical approaches like BoW, but predictive/probabilistic approaches such as vector embeddings may understand underlying patterns.</li> </ol> <h1 id="one-hot-vector-encoding-naive-approach">One-Hot Vector encoding (Naive Approach)</h1> <p>A naive way of making machines understand the semantic structure of our text corpus is by binary encoding. We can mark the position of each word inside a sentence in a binary vector, where the value of each element corresponds to a word in the sentence, and the value of the element signifies if the word is present at that location in the sentence (1) or not (0).</p> <p>Let us try to implement one-hot vector encoding the classic sentence : “The quick brown fox jumps over the lazy dog.”.</p> <p>To do this, we tokenize the sentence by whitespaces using <code class="language-plaintext highlighter-rouge">document.split()</code>. Then, we can check for unique words by using <code class="language-plaintext highlighter-rouge">set()</code> which would remove duplicate words. Then, we sort the words in alphabetical order.</p> <p>We can then produce a numpy matrix of size \(n \times m\), where \(n\) is the original number of words, and $$m$ is the number of unique words. Then we simply match each word in the document with our set of unique words and one-hot encode the position of the word inside the matrix.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">from</span> <span class="n">sklearn.feature_extraction.text</span> <span class="kn">import</span> <span class="n">CountVectorizer</span>

<span class="n">document</span> <span class="o">=</span> <span class="sh">"</span><span class="s">The quick brown fox jumps over the lazy dog.</span><span class="sh">"</span>

<span class="n">words</span> <span class="o">=</span> <span class="n">document</span><span class="p">.</span><span class="nf">split</span><span class="p">()</span>

<span class="n">unique_words</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="n">words</span><span class="p">)</span>

<span class="n">sorted_words</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span><span class="nf">list</span><span class="p">(</span><span class="n">unique_words</span><span class="p">))</span>

<span class="n">onehot_encoded</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">((</span><span class="nf">len</span><span class="p">(</span><span class="n">words</span><span class="p">),</span> <span class="nf">len</span><span class="p">(</span><span class="n">unique_words</span><span class="p">)),</span> <span class="nb">int</span><span class="p">)</span>

<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">words</span><span class="p">)):</span>
    <span class="n">onehot_encoded</span><span class="p">[</span><span class="n">i</span><span class="p">,</span> <span class="n">sorted_words</span><span class="p">.</span><span class="nf">index</span><span class="p">(</span><span class="n">words</span><span class="p">[</span><span class="n">i</span><span class="p">])]</span> <span class="o">=</span> <span class="mi">1</span>

<span class="nf">print</span><span class="p">(</span><span class="n">onehot_encoded</span><span class="p">)</span>

<span class="n">onehot_representation</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nc">DataFrame</span><span class="p">(</span><span class="n">onehot_encoded</span><span class="p">,</span> <span class="n">columns</span><span class="o">=</span><span class="n">sorted_words</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">onehot_representation</span><span class="p">)</span>
</code></pre></div></div> <p>This form of representation retains all the information about the grammar and order in a sentence. But note that this results in \(n\) vectors (given there are \(n\) words in the corpus) of \(m\) length because each word in the corpus is represented by a vector of \(m\) length, which is extremely a extremely high dimension-to-dataset ratio and will easily lead to the curse of dimensionality after adding just a few words/sentences.</p> <p>Such a method is ineffective when dealing with millions of tokens in a large text corpus, it will simply take up too much space and increase sparsity. We’re talking about \(n^2\) space complexity, so if you were to store a million bits worth of text (which is around 1500 pages according to <a href="https://medium.com/mlearning-ai/the-future-of-language-models-how-a-1-million-token-memory-could-revolutionize-ai-2-2c411267ed7e">this</a> source) using this method, you will need a million-million bits - roughly equivalent to 160TB of data.</p> <h1 id="bag-of-words-representation">Bag-of-Words Representation</h1> <p>The high dimensionality curse that incurred while using one-hot vectors was because we were using multiple dimensions to store the location of each word in a sentence. How important is that semantic location information? Can we get away with using a representation that does not care about the orientation of words? This is exactly what a technique called Bag-of-Words (BoW) explores.</p> <p>It is a common method of text representation which just directly stores the unique words in a text corpus and their frequencies. Let’s understand that by an example.</p> <p>“The quick brown fox jumps over the lazy dog” and “The dog barks at the fox.” If we represent these documents using bag-of-words model, we would get the following vectors:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Document 1: [2, 1, 1, 1, 1, 1] (the = 2, quick=1, brown=1, fox=1, jumps=1, over=1, lazy=1, dog = 1)
Document 2: [1, 1, 1, 1, 1, 1] (The=0, dog=1, barks=0, at=0, fox=0)
</code></pre></div></div> <p>This at first glance does not make sense and sounds frankly, quite stupid - because then sentences like “The dog cried and cat laughed.” and “The dog laughed and the cat cried” would be the exact same sentences since semantic order is not preserved. However, surprisingly enough, if we look at the bag of words representation of a sentence or even a big document, we can get some idea of what exactly the sentence is talking about - even though we may not get the correct context everytime. Similarly, machines can also do that. This technique is very useful when we are comparing two documents. It is also way more efficient than a one-hot-encoded representation of data. In fact, a BoW vector representation can be thought of as all the one-hot vectors added together into a singular vector. Bag of words is often the baseline metric against which other, more advanced techniques are compared.</p> <p>Let’s see how we can create and utilize BoW vectors - also called document-term matrices or DTMs. Creating DTMs is pretty easy using <code class="language-plaintext highlighter-rouge">CountVectorizer</code> which can allow us to directly create the DTM from our documents using the <code class="language-plaintext highlighter-rouge">fit_transform()</code> function. Then we simply put get the number of unique words or features.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.feature_extraction.text</span> <span class="kn">import</span> <span class="n">CountVectorizer</span>

<span class="n">documents</span> <span class="o">=</span> <span class="p">[</span>
    <span class="sh">"</span><span class="s">The quick brown fox jumps over the lazy dog</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">The dog barks at the fox</span><span class="sh">"</span>
<span class="p">]</span>

<span class="n">vectorizer</span> <span class="o">=</span> <span class="nc">CountVectorizer</span><span class="p">()</span>

<span class="n">bow_matrix</span> <span class="o">=</span> <span class="n">vectorizer</span><span class="p">.</span><span class="nf">fit_transform</span><span class="p">(</span><span class="n">documents</span><span class="p">)</span>

<span class="n">feature_names</span> <span class="o">=</span> <span class="n">vectorizer</span><span class="p">.</span><span class="nf">get_feature_names</span><span class="p">()</span>

<span class="nf">print</span><span class="p">(</span><span class="n">bow_matrix</span><span class="p">.</span><span class="nf">toarray</span><span class="p">())</span>

<span class="nf">print</span><span class="p">(</span><span class="n">feature_names</span><span class="p">)</span>

</code></pre></div></div> <p>This will produce the following output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[[0 0 1 1 1 1 1 1 1 2]
[1 1 0 1 1 0 0 0 0 2]]
['at' 'barks' 'brown' 'dog' 'fox' 'jumps' 'lazy' 'over' 'quick' 'the']
</code></pre></div></div> <p>As we can see, most words in 2 are also in 1, except the word “at” and “barks”.</p> <p>We can already see that the space that is required to store the two documents as DTMs is less (than half!) than the space required to store a single one-hot encoded document! This is why BoW is so efficient - it is linear storage - we require only \(n\) elements (where \(n\) is the total number of documents in all the documents fed to BoW) for this representation.</p> <p>Still, vanilla BoW representations have problems - they are extremely sparse still. This means there are way too many features and a lot of unique words are not represented enough (since they are too infrequent) - and words like “The” and “and” are more quite more represented.</p> <h2 id="zipfs-law-and-stop-words-removal">Zipf’s Law and Stop Words Removal</h2> <p>The frequency of occurance of a word in a corpus is in an inverse relationship with its rank in the frequency distribution. This is called Zipf’s law, and it is a statistical phenomenon.</p> <p>Zipf’s law dictates that if we rank all words in a corpus by their frequency of occurrence and plot the frequency against the rank, we would observe that the graph follows a power-law distribution with a slope of -1.</p> <p>For example, here is a famous plot of Zipf’s law for two english texts (source : <a href="https://en.wikipedia.org/wiki/Zipf%27s_law">Wikipedia</a>)</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset=" /me/assets/img/ZipfLaw-480.webp 480w, /me/assets/img/ZipfLaw-800.webp 800w, /me/assets/img/ZipfLaw-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/me/assets/img/ZipfLaw.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>By using Zipf’s law, we can estimate that a word is twice as more common in text than the word next in the distribution rank in the same text.</p> <p>Zipf’s law is directly tied to our sparseness problem - since it can be used to gauge the importance of a word in the corpus, based on its frequency. Words that are in the highest ranks in the Zipf’s law plot are generally conjunctive words, like “and” and “the” which do not add specific meaning to the sentence themselves. Such words are known as “Stop Words”. Unique words are much less common since they occupy but a small fraction of a sentence usually, and are much less repeated.</p> <p>Hence it is a good idea to trim out these stop words. There are several lists of stop words built into NLP libraries like <code class="language-plaintext highlighter-rouge">nltk</code> and <code class="language-plaintext highlighter-rouge">spacy</code> that can remove these stop word tokens from our corpus.</p> <p>We can simply import the stopwords corpus and then check our corpus against the stopwords. If any tokens match between the two, we remove them. Here is an implementation of the function to remove stopwords:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">remove_stopwords</span><span class="p">(</span><span class="n">doc</span><span class="p">):</span>
    <span class="n">stop_words</span> <span class="o">=</span> <span class="n">stopwords</span><span class="p">.</span><span class="nf">words</span><span class="p">(</span><span class="sh">'</span><span class="s">english</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s"> </span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">([</span><span class="n">word</span> <span class="k">for</span> <span class="n">word</span> <span class="ow">in</span> <span class="n">doc</span><span class="p">.</span><span class="nf">split</span><span class="p">()</span> <span class="k">if</span> <span class="n">word</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">stop_words</span><span class="p">])</span>

<span class="k">def</span> <span class="nf">remove_punctuation</span><span class="p">(</span><span class="n">doc</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">doc</span><span class="p">.</span><span class="nf">translate</span><span class="p">(</span><span class="nb">str</span><span class="p">.</span><span class="nf">maketrans</span><span class="p">(</span><span class="sh">''</span><span class="p">,</span> <span class="sh">''</span><span class="p">,</span> <span class="n">string</span><span class="p">.</span><span class="n">punctuation</span><span class="p">))</span>

</code></pre></div></div> <h2 id="stemming">Stemming</h2> <p>Often, we will have words that are actually the extension of the same root word. For example, “running” and “run” are extensions of the same word. The only difference is “ning”. However, a vanilla BoW will not know the difference between these two and hence we will end up adding features unnecessarily to our BoW.</p> <p>Here is where stemming comes into the picture. Stemming is the process of reducing words into their root form (called the “stem”), by eliminating the inflections on a word. The goal is to group together the different forms of a single word into one feature, hence reducing dimensionality. Stemming also has both a positive and negative effect on text classification and sentiment analysis tasks.</p> <p>There exist different forms of stemmers like Porter Stemmer, Snowball Stemmer, and Lanczos Stemmer.</p> <h3 id="porter-stemmer">Porter Stemmer</h3> <p>The porter stemmer algorithm was built in mind to enhance the efficiency of information retrieval tasks with limited reference from a stem dictionary, and instead was a heuristic based system which searched for generalized patterns to separate root words from suffixes. This use of heuristics, made porter stemmer faster than several other stemmers. However, using these heuristics it produces morphological variants of words, trading over phonetic accuracy.For example, happily \(\rarr\) happili. Although quite accurate in generalizing, it can too sometimes produce inaccurate results. For example, argument \(\rarr\) argument and is not stemmed. This stemmer tends to remove ‘e’ from the end of the sentence.</p> <p>We can use the porter stemmer in the following way by importing it from <code class="language-plaintext highlighter-rouge">nltk.stem</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">nltk.stem</span> <span class="kn">import</span> <span class="n">PorterStemmer</span>

<span class="n">stemmer</span> <span class="o">=</span> <span class="nc">PorterStemmer</span><span class="p">()</span>

<span class="n">words</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">argument</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">running</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">jumped</span><span class="sh">"</span><span class="p">]</span>

<span class="n">stemmed_words</span> <span class="o">=</span> <span class="p">[</span><span class="n">stemmer</span><span class="p">.</span><span class="nf">stem</span><span class="p">(</span><span class="n">word</span><span class="p">)</span> <span class="k">for</span> <span class="n">word</span> <span class="ow">in</span> <span class="n">words</span><span class="p">]</span>

<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Original words: </span><span class="si">{</span><span class="n">words</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Stemmed words: </span><span class="si">{</span><span class="n">stemmed_words</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>Running this will output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Original words: ['argument', 'running', 'jumped']
Stemmed words: ['argument', 'run', 'jump']
</code></pre></div></div> <h3 id="snowball-stemmer">Snowball Stemmer</h3> <p>Snowball stemmer is a family of stemmers, which is faster and more accurate version of the porter stemmer. It also supports multilingual stemming, meanwhile the original Porter stemmer only supported english. Hence it is often called the Porter2 stemmer. It has more hardcore rules than the porter stemmer but it also generalizes better.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ILY  -----&gt; ILI
LY   -----&gt; Nil
SS   -----&gt; SS
S    -----&gt; Nil
ED   -----&gt; E,Nil
</code></pre></div></div> <p>A snowball stemmer can be implemented similarly to how the porter stemmer is implemented.</p> <h3 id="lancaster-stemmer">Lancaster Stemmer</h3> <p>The Lancaster stemming algorithm works by analyzing the morphology of the words to reduce them to base form using a set of rules like other stemmers. The unique part about this algorithm is that it instead uses a lookup table to determine the correct stem for the word based on some rules. It is more flexible when handling words than other stemmers and can handle complex and compound words, as well as multilingual text. The lancaster stemmer also trims prefixes from the word aside suffixes.</p> <p>We can implement the lancaster stemmer in a similar fashion to Porter Stemmer by importing it from <code class="language-plaintext highlighter-rouge">nltk.stem</code>.</p> <h2 id="lemmatization">Lemmatization</h2> <p>Lemmatization is another NLP techinque to extract the base dictionary form, called a lemma, of a given word. This method is more context sensitive than stemming and allows us to represent the word’s meaning more accurately. Lemmatization uses a dictionary, called a morphological analyzer to accordingly decide what lemma to return for what specific word. It can also often differentiate between homonyms and understand context between the word. Like the word “saw” can be reduced to “see” or “saw” based on if it is a verb or a noun.</p> <p>However, most lemmatizers are costlier than stemmers to operate as they are less efficient. When our goal is to purely improve information retrieval and reduce dimensionality, stemmers often will do a better job quantitatively but lemmatizers would do a better job qualitatively. This means that stemmers are more efficient at compressing different inflection based variations of a word into one single stem, but the quality of reading the sentence may go down. Lemmatizers preserve morphological quality (readability) at the cost of being less efficient and proficient at reducing dimensionality.</p> <p>Let’s study how a lemmatizer can reduce words to a lemma by studying some different scenarios:</p> <ul> <li>‘better’ \(\rarr\) ‘good’</li> <li></li> </ul>]]></content><author><name></name></author><category term="lectures"/><category term="notes"/><category term="NLP"/><category term="CS6149"/><category term="Computer Science"/><summary type="html"><![CDATA[Notes for my NLP class at @NEU]]></summary></entry><entry><title type="html">CS6120 : Natural Language Processing | Week - 01</title><link href="https://snowclipsed.github.io//me/blog/2024/NLP-001/" rel="alternate" type="text/html" title="CS6120 : Natural Language Processing | Week - 01"/><published>2024-01-10T00:00:00+00:00</published><updated>2024-01-10T00:00:00+00:00</updated><id>https://snowclipsed.github.io//me/blog/2024/NLP-001</id><content type="html" xml:base="https://snowclipsed.github.io//me/blog/2024/NLP-001/"><![CDATA[<hr/> <h1 id="what-is-natural-language">What is Natural Language?</h1> <p>Natural language is the language that humans utilize to converse with each other in their day to day lives. Natural language can be of spoken and written form. It is a medium through which humans convey their ideas, thoughts, emotions, actions, and exchange information. Natural language can be thought of a programming language for our life where we interact with various modules to exchange information which allows us to perform tasks.</p> <p>Natural language is no single language, but it can be thought of as an umbrella term under which a collection of different languages that have evolved with society and humans themselves fall. Even though these languages may vastly differ from each other, most languages follow certain rules that apply to natural language and have some order to them.</p> <h2 id="why-are-computer-scientists-interested-in-natural-language">Why are computer scientists interested in Natural Language?</h2> <p>Since natural language is the way through which humans communicate and exchange information with each other. It would be incredibly useful to us as a society if machines were able to directly understand what we were saying and obey our commands in the language that we are fluent in natively, eliminating the need for mediums like complicated user interfaces.</p> <p>Speaking in natural language abstracts a lot of middleware and would make our lives way easier. Imagine a world where you are able to ask questions to machines as if they were conscious, like talking to your coffee machine or toaster in the morning. This is now partly possible becuase of the advancements in speech recognition.</p> <h2 id="what-is-natural-language-processing">What is Natural Language Processing?</h2> <p>Simply put, natural language processing or NLP is a collection of techniques that focus on making natural human language to be processed and understood by machines and making machines with the goal of them being able to extract information from the language like we do, and make decisions or perform tasks based on the extracted information. It involves techniques that allow us to help machines learn patterns in natural language and learn context.</p> <p>Context refers to the circumstances or information that surrounds a particular word, phrase, or statement and helps in determining its meaning. Context can be affected by different parts of the language and information contained by the language. This is the hard part for making machines understand natural language because it can vary so much. However, as we discussed before, language has some core ground rules.</p> <h2 id="what-are-the-components-of-a-language">What are the components of a language?</h2> <p>Language is a complex tool which we use to communicate and it has various components that are required to work together effectively to sustain proper communication. Themain components of a language include:</p> <ul> <li> <p><strong>Phonetics</strong> - It is how human percieve sound. It defines the correlation between the written medium and the spoken medium. The way something is spoken is often reflected in the way it is written in languages. For example, in the English language, consonants and vowels are a differentiation of letters based on how they are articulated. They can also be based on the loudness of the voice (acoustic), and based on how similar voices are percieved and differentiated (auditory).</p> </li> <li> <p><strong>Morphology</strong> - It is the study of words and the parts of a word, and the relationship of a word to other words. A <strong>morpheme</strong> is the smallest unit of meaning in a language. It can be a word (like cat, rat), or a part of a word(like a prefix, e.g. “un-“). It also studies homophony, where words that sound similar but have different meanings. Morphemes can also be blended together to form another word, like “black” and “board” form blackboard.</p> </li> <li> <p><strong>Syntax</strong> - It defines the rules which allows languages to have some consistency and structure to them so that they are interpreted easier. It governs the structure and arrangement of words and phrases so that they make coherent sense. Syntax is thought of as a subdivision of <strong>grammar</strong>, which is the entire set of rules for a language.</p> <ul> <li>Syntax includes things like parts of speech (nouns, adjectives, verbs, adverbs, etc.), phrasing and more.</li> <li>Sentences and phrases that look and sound similar may have different meanings because of syntactic ambiguity. It happens when there are more than one way of interpreting a given sentence, and the appropriate interpretation is understood only through context. For example, if someone says “Call me a cab.”, they might not be telling you to call them as “Cab” instead of their name, but to call a cab to pick them up. This is a major challenge for language models to understand cause it goes beyond basic syntax.</li> </ul> </li> <li> <p><strong>Semantics -</strong> Semantics deals with the meaning of words, phrases, sentences and language. Definitions of words as well as their meaning in relation to other words is studied through semantics.</p> <ul> <li>Sense relations: Words with similar meanings (synonyms), opposite meanings (antonyms), and homonyms, hypernyms are studied under semantics.</li> <li>Semantic field :Words that are related to each other and can be classified under a type can be said to be in the same semantic field. For example, roses and daisies are in the semantic field of flowers.</li> <li>Pragmatics : Pragmatics come into play when we look at how context and culture changes the meaning of a word or sentence. A sentence may have different connotations depending on where and how it is spoken or written.</li> <li>Semiotics : It is how symbols and colors are associated with certain meanings. For example, red and the skull may mean danger universally.</li> </ul> </li> </ul> <h1 id="formal-language">Formal Language</h1> <p>Amongst natural languages, there is a subset of languages with precise sets of rules and symbols which act as a guide on how to write them acceptably. Unlike natural languages, formal languages limit ambiguity and ensure clarity. They entail predictable behavior. Programming languages and mathematical equations can be called examples of formal languages. Formal languages can be used to express precise logic and denote repeatable tasks. This kind of language is much more interpretable by machines because of its predictable nature and defined set of rules on how to write and interpret them.</p> <h2 id="regular-expressions">Regular Expressions</h2> <p>Regular expressions or regex are sequences of characters which define a specific search pattern. They utilize a formal language called regular grammar. They are the basis of text search, text processing, and other string/literal manipulation tasks. A lot of programming languages have regex libraries for this reason. They can be utilized because a finite state machine is effectively able to process or parse such a language.</p> <p>Here is a small introduction to <a href="https://www.regular-expressions.info/quickstart.html">Regular Expressions</a> if you want to learn more.</p> <h1 id="tokenization">Tokenization</h1> <p>As we discussed, machines do not understand sentences directly. Since sentences are complex, it is a bad idea to feed the machines sentences as information chunks directly because we will then lose the more granular detail in those sentences. Hence it is a good approach to break sentences up into smaller parts. This is called <strong>tokenization</strong>, where we break down text into its component units called <strong>“tokens”</strong>. Tokenization is a crucial step in modern NLP tasks and helps natural language understanding.</p> <h2 id="types-of-tokens">Types of Tokens</h2> <ol> <li> <p><strong>Word-based</strong> <strong>:</strong> This is one of the most naive ideas we have when we think about splitting text into tokens. When we assume words as tokens, we break down a sentence into.. well you guessed it, its component words.</p> <p>​ For example : “The quick brown fox jumps over the lazy dog” would be tokenized as [“The”, “quick”, “brown”, “fox”, “jumps”, “over”, “the”, “lazy”, “dog”].</p> <p>To perform word-based tokenization we will be using the popular <code class="language-plaintext highlighter-rouge">nltk</code> library:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="kn">import</span> <span class="n">nltk</span>
 <span class="n">text</span> <span class="o">=</span> <span class="sh">"</span><span class="s">This is a sentence with some words.</span><span class="sh">"</span>
 <span class="n">tokens</span> <span class="o">=</span> <span class="n">nltk</span><span class="p">.</span><span class="nf">word_tokenize</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>
 <span class="nf">print</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>
</code></pre></div> </div> <p>Output will be <code class="language-plaintext highlighter-rouge">['This', 'is', 'a', 'sentence', 'with', 'some', 'words']</code></p> </li> <li> <p><strong>Phrase-based :</strong> This form of tokenization divides sentences into chunks by grouping words that are semantically and contextually related into a single phrase. This allows the machine to understand which words are related to the same semantic field.</p> </li> <li> <p><strong>Morpheme-based :</strong> This form of tokenization assumes that tokens are morphemes, which are the smallest units of a sentence that carry meaning. They are smaller than words. For example, the word “running” can be broken down into the morphemes “run”, which is the base form of the verb, and “-ing”. Such forms of tokenization can be useful in a process called “<strong>stemming</strong>”, which means reducing a word to its root form, as well as sentiment analysis. It is more complex than word based tokenization because it requires the knowledge of the morphological properties of the language.</p> </li> <li> <p><strong>Sentence-based :</strong> This form of tokenization is based on splitting large paragraphs into particular sentences. The final token list formed is an array of sentences.</p> </li> </ol> <p>To perform sentence-based tokenization, we can use <code class="language-plaintext highlighter-rouge">nltk</code> again</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">nltk</span>
<span class="n">text</span> <span class="o">=</span> <span class="sh">"</span><span class="s">This is a sentence with some words, including a phrase.</span><span class="sh">"</span>
<span class="n">sentences</span> <span class="o">=</span> <span class="n">nltk</span><span class="p">.</span><span class="nf">sent_tokenize</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">sentences</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>['This is a sentence with some words, including a phrase.', '']
</code></pre></div></div> <ol> <li><strong>n-gram tokens:</strong> This form of tokenization assumes that a token is made of ‘n’ number of consecutive words. For example, when n=2, also known as <strong>bi-gram</strong>, the sentence is divided into groups of two, and in n=3 (trigram), they will be divided into groups of 3. The purpose of including multiple words is to capture the context between the words, which can be useful for tasks like sentiment analysis. n-gram tokenization is useful but also has certain disadvantages. They are not useful to capture context when n becomes sufficiently large. Hence, n-grams are bad at generalizing context of the entire text or even sentence. They are also a higher dimensional representation of text, hence it can be hard to work with n-gram representation when dealing with a large text corpus.</li> </ol> <p>We can build a simple n-gram tokenizer using the word tokenizer from <code class="language-plaintext highlighter-rouge">nltk</code> and then grouping up the words together.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">nltk</span>
<span class="kn">from</span> <span class="n">collections</span> <span class="kn">import</span> <span class="n">Counter</span>

<span class="k">def</span> <span class="nf">n_gram_tokenizer</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">1</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    N-gram tokenizer for text.
    
    Parameters:
    text (str): The text to tokenize.
    n (int): The number of items in each n-gram. Default is 1.
        
    Returns:
    list: A list of n-grams found in the text.
    </span><span class="sh">"""</span>
    
    <span class="c1"># Split the text into individual words
</span>    <span class="n">tokens</span> <span class="o">=</span> <span class="n">nltk</span><span class="p">.</span><span class="nf">word_tokenize</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>
    
    <span class="c1"># Create a dictionary to store each n-gram and its count
</span>    <span class="n">n_gram_dict</span> <span class="o">=</span> <span class="nc">Counter</span><span class="p">()</span>
    
    <span class="c1"># Iterate through each window of length n
</span>    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span><span class="o">-</span><span class="n">n</span><span class="o">+</span><span class="mi">1</span><span class="p">):</span>
        <span class="c1"># Get the substring at index i and convert it to a tuple
</span>        <span class="n">n_gram</span> <span class="o">=</span> <span class="nf">tuple</span><span class="p">(</span><span class="n">tokens</span><span class="p">[</span><span class="n">i</span><span class="p">:</span><span class="n">i</span><span class="o">+</span><span class="n">n</span><span class="p">])</span>
        
        <span class="c1"># If the n-gram is not already in the dictionary, add it with a count of 1
</span>        <span class="k">if</span> <span class="n">n_gram</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">n_gram_dict</span><span class="p">:</span>
            <span class="n">n_gram_dict</span><span class="p">[</span><span class="n">n_gram</span><span class="p">]</span> <span class="o">=</span> <span class="mi">1</span>
        <span class="c1"># Otherwise, increment the count for that n-gram
</span>        <span class="k">else</span><span class="p">:</span>
            <span class="n">n_gram_dict</span><span class="p">[</span><span class="n">n_gram</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
            
    <span class="c1"># Convert the dictionary to a list of tuples and return it
</span>    <span class="k">return</span> <span class="nf">list</span><span class="p">(</span><span class="n">n_gram_dict</span><span class="p">.</span><span class="nf">items</span><span class="p">())</span>

</code></pre></div></div> <p>Here, the function is basically a loop which runs across the token array and then creates a tuple of adjacent tokens.</p> <h1 id="methods-of-tokenization">Methods of Tokenization</h1> <ol> <li> <p><strong>Rule-based</strong> : This involves using a specific rule to break text into tokens. These rules can be regex-based ; as in splitting the text by whitespace or punctuation, or by a word. It is useful when the text is irregular or there are too many grammatical mistakes. This form of tokenization is deliberate and hence is sometimes prone to producing unwanted results. For example, sometimes whitespace-based tokenization may result into a faulty token sequence.</p> </li> <li> <p><strong>Statistical tokenization :</strong> This method uses probabilistic models to predict where tokens should be placed in a sentence. These models can be trained on large corpora and use statistical patterns to determine word breaks. These approaches work when pattern-based approaches fail.</p> </li> <li> <p><strong>Machine/Deep learning-based tokenization :</strong> This method involves using deep learning techniques such as recurrent neural networks (RNNs) or long short-term memory (LSTM) networks to automatically segment text into tokens based on syntactic and semantic features. These models have shown to be effective in handling complex sentence structures and contextual dependencies.</p> </li> </ol> <h1 id="goals-for-week-2">Goals for Week 2</h1> <ul> <li>Complete and submit Assignment 0.</li> <li>Topics to be covered next lecture, we will build upon tokenization : <ul> <li>Stemming &amp; Lemmatization, Text Normalization</li> <li>Language Model Estimation, Smoothing Techniques, Zips’ law</li> <li>Rule-Based and Statistical POS Tagging</li> </ul> </li> </ul>]]></content><author><name></name></author><category term="lectures"/><category term="notes"/><category term="NLP"/><category term="CS6149"/><category term="Computer Science"/><summary type="html"><![CDATA[Notes for my NLP class at @NEU]]></summary></entry><entry><title type="html">SatTrack</title><link href="https://snowclipsed.github.io//me/blog/2023/SatTrack/" rel="alternate" type="text/html" title="SatTrack"/><published>2023-12-12T00:00:00+00:00</published><updated>2023-12-12T00:00:00+00:00</updated><id>https://snowclipsed.github.io//me/blog/2023/SatTrack</id><content type="html" xml:base="https://snowclipsed.github.io//me/blog/2023/SatTrack/"><![CDATA[<p>Redirecting to another page.</p>]]></content><author><name></name></author><category term="projects"/><category term="API"/><category term="GIS"/><category term="REST"/><category term="Orbital Mechanics"/><category term="Java"/><category term="CS5010"/><category term="Computer Science"/><summary type="html"><![CDATA[A satellite tracker written in Java.]]></summary></entry><entry><title type="html">CS5800 : Algorithms - 01</title><link href="https://snowclipsed.github.io//me/blog/2023/CS5800-01/" rel="alternate" type="text/html" title="CS5800 : Algorithms - 01"/><published>2023-10-08T00:00:00+00:00</published><updated>2023-10-08T00:00:00+00:00</updated><id>https://snowclipsed.github.io//me/blog/2023/CS5800-01</id><content type="html" xml:base="https://snowclipsed.github.io//me/blog/2023/CS5800-01/"><![CDATA[<h2 id="what-is-an-algorithm">What is an Algorithm?</h2> <p>We see the word <strong>Algorithm</strong> pop up everywhere. However, the meaning of an algorithm is often only superficially explored upon. What exactly is an algorithm? It may be hard to come up with an exact definition - but I realize this probably is a consequence of it being such a loosely used word.</p> <p>But when you study computer science, you always deal with algorithms. It’s important to have a specific definition of something so important.</p> <p>We can describe an algorithm as a finite list of steps that allow us to achieve some goal or solve a particular problem. It has an input and an output. An algorithm should always be finite by definition, i.e. ; it should halt and produce a result. Most things we do are by following an algorithm. A recipe is an algorithm, so is how we tie laces. The way we think is often also an algorithm.</p> <p>Here’s a real life algorithm on how a person may decide on a place to eat :</p> <pre><code class="language-mermaid">graph TD
	id1[Time of Day] --&gt; Morning 
	id1[Time of Day] --&gt; Afternoon 
	id1[Time of Day] --&gt; Night
	Morning --&gt; id2
  id2[Hunger Level?] --&gt; High
	id2[Hunger Level?] --&gt; Low
	High --&gt; id3[Egg Toast]
	Low --&gt; Banana
</code></pre> <h2 id="analysis-of-algorithms">Analysis of Algorithms</h2> <p>An algorithm must have properties that can be ascertained through qualitative or quantitative analysis. One such measure is the <em>correctness</em> of an algorithm, another is the <em>efficiency</em> and <em>scalability</em>. Analyzing algorithms is important because it can help us identify what algorithm may work best for a given task, improve existing algorithms as well as investigate different scenarios.</p> <p>A very popular method to analyze algorithms is called <em>Asymptotic Analysis</em>.</p> <h2 id="asymptotic-analysis">Asymptotic Analysis</h2> <p>Asymptotic analysis of a function allows us to analyze how any given function or an algorithm grows when its input grows larger and larger, also called the computational complexity of an algorithm. It helps us analyze how resource intensive an algorithm is. It is a more objective and standardized way of analyzing an algorithm’s performance.</p> <p>The complexity of an algorithm can be measured in various attributes :</p> <ol> <li><strong>Time Complexity :</strong> It is the time an algorithm will take to complete execution as a function of a given input size.</li> <li><strong>Space Complexity :</strong> It is the space an algorithm will be requiring to execute completely as a function of input size.</li> </ol> <p>Asymptotic analysis is described using <strong>asymptotic notation</strong>, which is a form of mathematical notation. Any asymptotic informal notation for a given function \(f(n)\)$ is of the form \(X(g(n))\), where \(X\)$ is one of the asymptotic notations that will be discussed below. There are different types of asymptotic notations to describe function growth in different ways. Asymptotic notation is comparative; i.e.; it compares the function to others and tries to determine if the given function scales similarly to the other said functions.</p> <p>Finally, these are the most asymptotic notations most commonly dealt with :</p> <ul> <li><strong>Big-Oh Notation : \(O(g(n))\)</strong></li> <li><strong>Omega Notation : \(\Omega(g(n))\)</strong></li> <li><strong>Theta Notation : \(\Theta(g(n))\)</strong></li> <li><strong>Little-Oh Notation : \(o(g(n))\)</strong></li> <li><strong>Little-Omega Notation : \(\omega(g(n))\)</strong></li> </ul> <h3 id="big-oh-notation">Big-Oh Notation</h3> <p>The Big-Oh notation describes a function \(g(n)\) for every function \(f(n)\), such that when \(g(n)\) is multiplied by a positive constant <em>c</em>, it acts as the upper bound on a function’s growth rate for any sufficiently large input. It can also be called the worst possible way a function may scale with increasing size of inputs. In \(O\) notation, the \(O(g(n))\) is pronounced as “order of” the argument function \(g(n)\).</p> <p>Through this information, we can denote this mathematically :</p> <p>Suppose we have an input size of \(n\), where \(\forall n\ge n_0\)</p> <p>Then, if \begin{equation} f(n) \le c* g(n)<br/> \Rightarrow f(n) = O(g(n)) \end{equation}</p> <p>Let’s consider the function \(f(n) = n^2 + 1\). If \(n = 1\), the function outputs 2. If \(n = 2\), the function outputs 5. If \(n = 3\ , f(n) = 29\). If we plot the function, we can see that it grows similarly to \(g(n) = n^2\). Here, the red curve is \(g(n) = n^2 + 1\). Meanwhile, the green curve is \(f(n) = n^2\). We can see that both functions somewhat scale similarly.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset=" /me/assets/img/bigohexample2-480.webp 480w, /me/assets/img/bigohexample2-800.webp 800w, /me/assets/img/bigohexample2-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/me/assets/img/bigohexample2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>However, right now the function \(f(n)\) seems to scale larger than \(g(n)\). Let’s consider the condition for Big-Oh notation again : \(f(n) \le c* g(n) \Rightarrow f(n) = O(g(n))\). Hence if we multiply \(g(n)\) by a constant, say \(c=2\), we get this:</p> <p>This in fact, is the order of the function \(f(n)\), an abstracted way of understanding how the function grows by comparing it to other simple functions. Appropriately, \(g(n)\) grows in the order of itself : as in $$O(n^2 +1 )$. However, when doing asymptotic analysis - we largely tend to ignore the lower order terms since they offer little contribution to how the function scales with big input sizes.</p> <p>Let’s look at the same example through the help of a graph.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset=" /me/assets/img/bigohexample-480.webp 480w, /me/assets/img/bigohexample-800.webp 800w, /me/assets/img/bigohexample-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/me/assets/img/bigohexample.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Now, \(g(n)\) scales faster than \(f(n)\) after \(n \ge 2\), hence \(n_0 = 2\). This means \(g(n)\) will always act as the upper bound for \(f(n)$. Hence we\)f(n)\(to be\)O(n^2)$.</p> <h4 id="whys-big-oh-notation-useful">Why’s Big-Oh Notation Useful?</h4> <p>Big-Oh notation is perhaps the most used form of complexity analysis in the scientific and professional world. This is done because of its direct definition : it offers an upper bound on the function’s complexity. This is why when we talk about something running in $O(n)$, we know that the complexity of said program is at most linear. There’s a lot of runtimes that are popular benchmarks for programs.</p> <h5 id="popular-runtimes">Popular Runtimes</h5> <table> <thead> <tr> <th style="text-align: center">Notation</th> <th style="text-align: center">Runtime</th> </tr> </thead> <tbody> <tr> <td style="text-align: center">\(O(1)\)</td> <td style="text-align: center">Constant Time</td> </tr> <tr> <td style="text-align: center">\(O(logn)\)</td> <td style="text-align: center">Logarithmic Time</td> </tr> <tr> <td style="text-align: center">\(O(n)\)</td> <td style="text-align: center">Linear Time</td> </tr> <tr> <td style="text-align: center">\(O(nlogn)\)</td> <td style="text-align: center">Linearithmic Time</td> </tr> <tr> <td style="text-align: center">\(O(n^2)\)</td> <td style="text-align: center">Quadratic Time</td> </tr> <tr> <td style="text-align: center">\(O(n^k)\)</td> <td style="text-align: center">Polynomial Time</td> </tr> <tr> <td style="text-align: center">\(O(2^n)\)</td> <td style="text-align: center">Exponential Time</td> </tr> <tr> <td style="text-align: center">\(O(n!)\)</td> <td style="text-align: center">Factorial Time</td> </tr> </tbody> </table> <h3 id="big-omega-notation-omega"><strong>Big-Omega Notation \((\Omega)\)</strong></h3> <p>In some ways, \(\Omega\)-notation is the opposite of Big-Oh notation. \(\Omega\)-notation of a function \(f(n)\) provides us with the lower bound of the function’s runtime instead of the upper bound. Together with \(O\) notation, it provides a more complete asymptotic analysis of a function’s performance in runtime.</p> <p>\(\Omega\)-notation can be represented mathematically :</p> \[0 \le c*g(n) \le f(n) \ , \forall n &gt;= n_0\] \[\Rightarrow f(n) = \Omega(g(n))\] <p>Hence, there exists a function \(g(n)\) for a given function \(f(n)\), such that when \(g(n)\) is multiplied by a positive constant \(c\), it is lower than or equal to the function \(f(n)\) and acts as a lower bound on the function. Then, \(f(n) = \Omega(g(n))\).</p> <p>Let’s try to explain \(\Omega\)-notation and \(O\)-notation through an example. Again, let’s assume \(f(n) = n^2+1\). Through the previous example when explaining \(O\)-notation we established how this expression is \(O(n^2)\). For the \(\Omega\)-notation, let’s take a function \(g(n)=n\). Substituting \(f(n)\) and \(g(n)\) the definition of \(\Omega\)-notation:</p> \[0 \le c*n \le n^2+1 \ , \forall n &gt;= n_0\] <p>We can take \(c=2\) and \(n_0 = 1\) and we will have :</p> \[0 \le 2n \le n^2+1 \ , \forall n &gt;= 1\] <p>Since the inequality stands true, \(f(n) = \Omega(n)\).</p> <p>Let’s try plotting this inequality in desmos:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset=" /me/assets/img/omegaexample-480.webp 480w, /me/assets/img/omegaexample-800.webp 800w, /me/assets/img/omegaexample-1400.webp 1400w, " sizes="95vw" type="image/webp"/> <img src="/me/assets/img/omegaexample.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Where, \(f(n) = n^2+1\) is the red line and \(g(n) = n\) is the green one.</p> <p>This means the function \(g(n) = n\) is the lower bound of \(f(n)\) ; i.e.; the function \(f(n)\) will take <strong>at least</strong> as much time as \(g(n) = n\), but may take more. For $O$-notation, if the function \(f(n)\) will take <strong>at most</strong> as much time as \(g(n) = n^2\).</p> <h3 id="theta-notation-theta">Theta-Notation (\(\Theta\))</h3> <p>\(\Theta\)-notation is a tight bound on a function : it is the combination of the high and low bounds. It denotes an algorithm’s performance in a well defined and specific range. It is meant to be a more precise tool for asymptotic analysis.</p> <p>If we’re given a function \(f(n)\) and another function \(g(n)\), and we can define constants \(c_1, c_1\) and \(n_0\) such that , \(\forall n\ge n_0\): \(0 \le c_2*g(n) \le f(n)\)</p> \[0\le f(n) \le c_1*g(n)\] <p>then, \(f(n) = \Theta(g(n))\)</p> <p>The function \(f(n)\) is said to be sandwiched between the two bounds here, hence it’s tightly fixed in place.</p> <p>From our previous example with \(f(n) = n^2 +1\), if we have \(g(n) = n^2\) , we can put \(c_1 = 1, c_2 = 2\) and \(n_0 = 1\) : \(0\le 2*n^2 \le n^2+1\)</p> \[0\le n^2 +1 \le 1*n^2\] <p>Hence, \(f(n) = \Theta(n^2)\)</p>]]></content><author><name></name></author><category term="lectures"/><category term="notes"/><category term="algorithms"/><category term="CS5800"/><category term="Computer Science"/><summary type="html"><![CDATA[What is an Algorithm?]]></summary></entry></feed>